<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CustomControl">

    <!-- ESTILO PARA LA TARJETA INFOCARD -->
    <Style TargetType="{x:Type local:InfoCard}">

        <!-- Valores por defecto -->
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#E5E5E5"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Height" Value="100"/>

        <!-- Sombra suave -->
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#DDDDDD" Opacity="0.5"/>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:InfoCard}">
                    <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="12"
                        Effect="{TemplateBinding Effect}">

                        <Grid Margin="15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <!-- Icono -->
                                <ColumnDefinition Width="*"/>
                                <!-- Texto -->
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" 
                                Width="50" Height="50"
                                CornerRadius="25"
                                Background="{TemplateBinding IconBackground}"
                                VerticalAlignment="Center"
                                Margin="0,0,15,0">
                                <!-- El Path dibuja el icono vectorial -->
                                <Path Data="{TemplateBinding Icon}"
                                  Fill="White"
                                  Stretch="Uniform"
                                  Width="24" Height="24"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                            </Border>

                            <!-- TEXTOS -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <!-- Valor grande (ej. 150) -->
                                <TextBlock Text="{TemplateBinding Value}" 
                                       FontSize="26" 
                                       FontWeight="Bold" 
                                       Foreground="#333333"/>

                                <TextBlock Text="{TemplateBinding Title}" 
                                       FontSize="14" 
                                       Foreground="#888888"
                                       Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="20" ShadowDepth="5" Direction="270" Color="#CCCCCC" Opacity="0.7"/>
                                </Setter.Value>
                            </Setter>
                            <!-- Escala ligera al pasar el mouse -->
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.02" ScaleY="1.02" CenterX="150" CenterY="50"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>